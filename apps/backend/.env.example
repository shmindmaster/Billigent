# Backend environment configuration (example - do not commit secrets)
PORT=3001
DATABASE_URL="sqlserver://<server>.database.windows.net:1433;database=<db>;user=<user>;password=<strong-password>;encrypt=true"
AZURE_COSMOS_ENDPOINT="https://<cosmos-account>.documents.azure.com:443/"
AZURE_COSMOS_KEY="<cosmos-key>" # store in Azure Key Vault in production
AZURE_COSMOS_DATABASE="billigent"
SAFE_MODE=false
# Sentry instrumentation
SENTRY_DSN="https://<public>@o<org>.ingest.us.sentry.io/<project>"
SENTRY_TRACES_SAMPLE_RATE=0.2
SENTRY_PROFILES_SAMPLE_RATE=0.1
APP_RELEASE=backend@0.1.0
