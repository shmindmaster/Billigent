// Minimal global setup for Playwright - no-op to satisfy config
export default async function globalSetup() {
  // Intentionally empty; dev servers are managed by Playwright webServer or already running
}


